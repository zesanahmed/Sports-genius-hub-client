type CardProps = {
  title: string;
  img: string;
  index: number;
};

const MembershipCard = ({ title, img, index }: CardProps) => {
  return <div className=" w-64 h-96 bg-white"></div>;
};

export default MembershipCard;
